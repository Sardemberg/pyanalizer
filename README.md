# pyanalizer
To the project work, it's necessery follow libs:
    1. Redis
    2. Celery
    3. Pickle
    
## Instalation:
    - Installing redis:
        ```
        sudo apt install redis-server
        pip install redis
        ```
